#!/usr/bin/env bash
set -euo pipefail

echo "Reset database interface"
echo "TODO T04: reset PostgreSQL schema after migrations exist."
echo "Current safe action: docker compose down -v can be run manually when no app data must be preserved."
exit 1
